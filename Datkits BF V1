repeat task.wait() until game:IsLoaded() and game.Players.LocalPlayer

-------------------------------------------------
-- LOAD FLUENT UI & ADDONS
-------------------------------------------------
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Datkits Premium | Blox Fruits",
    SubTitle = "Target Recognition + Auto Farm + Auto Fruit",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 480),
    Acrylic = false,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Main = Window:AddTab({ Title = "Auto Farm", Icon = "sword" }),
    Player = Window:AddTab({ Title = "Nhân Vật", Icon = "user" }),
    Settings = Window:AddTab({ Title = "Settings", Icon = "settings" })
}

-------------------------------------------------
-- BIẾN TOÀN CỤC
-------------------------------------------------
getgenv().AutoQuest = false
getgenv().AutoFarm = false
getgenv().AutoFruit = false
getgenv().AutoStoreFruit = true
getgenv().FastAttack = true
getgenv().FarmDistance = 6
getgenv().TweenSpeed = 260
getgenv().FruitTweenSpeed = 300
getgenv().TargetPrioritize = "nearest"

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local currentTween = nil
local lastTargetPos = nil
local noclipConnection = nil
local isGoingToFarmArea = false

-------------------------------------------------
-- HÀM CƠ BẢN
-------------------------------------------------
local function GetCharacter()
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") and char.Humanoid.Health > 0 then
        return char
    end
    return nil
end

local function GetLevel()
    local lv = 1
    pcall(function() lv = LocalPlayer.Data.Level.Value end)
    return lv
end

local function GetCurrentSea()
    local lv = GetLevel()
    if lv >= 1500 then return 3
    elseif lv >= 700 then return 2
    else return 1 end
end

local function StopTween()
    if currentTween then
        pcall(function() currentTween:Cancel() end)
        currentTween = nil
    end
    lastTargetPos = nil
    isGoingToFarmArea = false

    local char = GetCharacter()
    if char then
        if char:FindFirstChild("Humanoid") then
            char.Humanoid.PlatformStand = false
        end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.AssemblyLinearVelocity = Vector3.zero
        end
    end
end

local function ResetCharacterState()
    StopTween()
    local char = GetCharacter()
    if not char then return end
    for _, part in ipairs(char:GetDescendants()) do
        if part:IsA("BasePart") then
            part.CanCollide = true
        end
    end
end

local function ToggleNoclip(enable)
    if enable then
        if not noclipConnection then
            noclipConnection = RunService.Stepped:Connect(function()
                local char = GetCharacter()
                if char then
                    for _, part in ipairs(char:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end
            end)
        end
    else
        if noclipConnection then
            noclipConnection:Disconnect()
            noclipConnection = nil
        end
    end
end

local function TweenTo(cf, speed)
    speed = speed or getgenv().TweenSpeed or 260
    local char = GetCharacter()
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if not hrp or not hum then return end

    hum.PlatformStand = true

    for _, part in ipairs(char:GetDescendants()) do
        if part:IsA("BasePart") then
            part.CanCollide = false
        end
    end

    local dist = (hrp.Position - cf.Position).Magnitude
    if dist <= 5 then
        StopTween()
        hrp.CFrame = cf
        isGoingToFarmArea = false
        return
    end

    if isGoingToFarmArea and lastTargetPos and (lastTargetPos - cf.Position).Magnitude < 12 then
        return
    end

    lastTargetPos = cf.Position

    if currentTween then
        pcall(function() currentTween:Cancel() end)
    end

    local duration = math.clamp(dist / speed, 0.1, 7)
    currentTween = TweenService:Create(hrp, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = cf})
    currentTween:Play()
end

local function EquipWeapon()
    local char = GetCharacter()
    if not char or char:FindFirstChildOfClass("Tool") then return end

    for _, tool in ipairs(LocalPlayer.Backpack:GetChildren()) do
        if tool:IsA("Tool") then
            local n = tool.Name:lower()
            if tool.ToolTip == "Melee" or n:find("combat") or n:find("step") or n:find("karate")
            or n:find("dragon") or n:find("god") or n:find("superhuman") or n:find("death") then
                char.Humanoid:EquipTool(tool)
                return
            end
        end
    end
    local any = LocalPlayer.Backpack:FindFirstChildOfClass("Tool")
    if any then char.Humanoid:EquipTool(any) end
end

local function HasQuest()
    local ok, res = pcall(function()
        local main = LocalPlayer.PlayerGui:FindFirstChild("Main")
        if not main then return false end
        local quest = main:FindFirstChild("Quest")
        return quest and quest.Visible
    end)
    return ok and res
end

-------------------------------------------------
-- QUEST DATA
-------------------------------------------------
local function GetQuestData()
    local level = GetLevel()
    local sea = GetCurrentSea()

    if sea == 1 then
        if level <= 9 then return "BanditQuest", 1, "Bandit", CFrame.new(1059.37, 16.51, 1549.26), CFrame.new(1145, 17, 1634)
        elseif level <= 14 then return "JungleQuest", 1, "Monkey", CFrame.new(-1598.08, 36.85, 153.38), CFrame.new(-1610, 37, 145)
        elseif level <= 29 then return "JungleQuest", 2, "Gorilla", CFrame.new(-1598.08, 36.85, 153.38), CFrame.new(-1235, 6, -486)
        elseif level <= 39 then return "BuggyQuest1", 1, "Pirate", CFrame.new(-1140.17, 4.75, 3827.41), CFrame.new(-1215, 5, 3915)
        elseif level <= 59 then return "BuggyQuest1", 2, "Brute", CFrame.new(-1140.17, 4.75, 3827.41), CFrame.new(-1145, 15, 4350)
        elseif level <= 89 then return "DesertQuest", 1, "Desert Bandit", CFrame.new(894.49, 6.44, 4384.58), CFrame.new(932, 6, 4485)
        elseif level <= 119 then return "SnowQuest", 1, "Snow Bandit", CFrame.new(1385.84, 87.27, -1298.61), CFrame.new(1285, 105, -1342)
        elseif level <= 149 then return "MarineQuest2", 1, "Chief Petty Officer", CFrame.new(-5039.59, 28.65, 4324.78), CFrame.new(-4855, 20, 4260)
        elseif level <= 189 then return "SkyQuest", 1, "Sky Bandit", CFrame.new(-4839.53, 717.67, -2629.09), CFrame.new(-4975, 717, -2885)
        elseif level <= 224 then return "PrisonerQuest", 1, "Prisoner", CFrame.new(530.81, 1.66, 474.98), CFrame.new(540, 2, 485)
        elseif level <= 299 then return "ColosseumQuest", 1, "Toga Warrior", CFrame.new(-1588.34, 7.39, -2986.48), CFrame.new(-1800, 7, -2980)
        elseif level <= 374 then return "MagmaQuest", 1, "Military Soldier", CFrame.new(-5313.37, 8.58, 8515.29), CFrame.new(-5400, 9, 8530)
        elseif level <= 449 then return "FishmanQuest", 1, "Fishman Warrior", CFrame.new(61163.85, 18.5, 1569.26), CFrame.new(60800, 18, 1500)
        elseif level <= 524 then return "SkyQuest2", 1, "God's Guard", CFrame.new(-7861.34, 5545.49, -380.29), CFrame.new(-7725, 5600, -440)
        elseif level <= 624 then return "SkyQuest2", 2, "Shandian", CFrame.new(-7861.34, 5545.49, -380.29), CFrame.new(-7850, 5600, -220)
        else return "FountainQuest", 1, "Galley Pirate", CFrame.new(5259.82, 38.5, 4050.03), CFrame.new(5580, 38, 3990) end
    end

    if sea == 2 then
        if level < 725 then return "Area1Quest", 1, "Raider", CFrame.new(-429.54, 71.77, 1836.18), CFrame.new(-728, 52, 2345)
        elseif level < 775 then return "Area1Quest", 2, "Mercenary", CFrame.new(-429.54, 71.77, 1836.18), CFrame.new(-1004, 80, 1424)
        elseif level < 800 then return "Area2Quest", 1, "Swan Pirate", CFrame.new(638.44, 71.77, 918.28), CFrame.new(1068, 137, 1322)
        elseif level < 875 then return "Area2Quest", 2, "Factory Staff", CFrame.new(638.44, 71.77, 918.28), CFrame.new(882, 110, -49)
        elseif level < 900 then return "MarineQuest3", 1, "Marine Lieutenant", CFrame.new(-2440.79, 71.71, -3216.07), CFrame.new(-2552, 110, -3050)
        elseif level < 950 then return "MarineQuest3", 2, "Marine Captain", CFrame.new(-2440.79, 71.71, -3216.07), CFrame.new(-2552, 110, -3050)
        elseif level < 975 then return "ZombieQuest", 1, "Zombie", CFrame.new(-5494.34, 48.52, -795.16), CFrame.new(-5550, 48, -800)
        else return "ZombieQuest", 2, "Vampire", CFrame.new(-5494.34, 48.52, -795.16), CFrame.new(-5550, 48, -800) end
    end

    return "Area1Quest", 1, "Raider", CFrame.new(-429.54, 71.77, 1836.18), CFrame.new(-728, 52, 2345)
end

-------------------------------------------------
-- HỆ THỐNG NHẬN DIỆN MỤC TIÊU
-------------------------------------------------
local function GetAllAliveMonsters()
    local list = {}
    local enemies = workspace:FindFirstChild("Enemies")
    if not enemies then return list end

    for _, enemy in ipairs(enemies:GetChildren()) do
        local hum = enemy:FindFirstChild("Humanoid")
        local hrp = enemy:FindFirstChild("HumanoidRootPart")
        if hum and hrp and hum.Health > 0 then
            table.insert(list, {
                model = enemy,
                hrp = hrp,
                humanoid = hum,
                name = enemy.Name,
                lowerName = string.lower(enemy.Name),
                hp = hum.Health,
                level = enemy:GetAttribute("Level") or 1
            })
        end
    end
    return list
end

local function IsQuestMonster(monster, questMobName)
    if not questMobName or questMobName == "" then return false end
    return string.find(monster.lowerName, string.lower(questMobName), 1, true) ~= nil
end

local function FindBestTarget(charPos, questMobName, prioritize, maxDistance)
    prioritize = prioritize or getgenv().TargetPrioritize or "nearest"
    maxDistance = maxDistance or 5000

    local allMonsters = GetAllAliveMonsters()
    if #allMonsters == 0 then return nil end

    local questTargets = {}
    local nearestTargets = {}

    for _, mon in ipairs(allMonsters) do
        local dist = (mon.hrp.Position - charPos).Magnitude
        if dist <= maxDistance then
            if IsQuestMonster(mon, questMobName) then
                table.insert(questTargets, mon)
            end
            table.insert(nearestTargets, mon)
        end
    end

    local candidates = #questTargets > 0 and questTargets or nearestTargets
    if #candidates == 0 then return nil end

    table.sort(candidates, function(a, b)
        if prioritize == "weakest" then
            return a.hp < b.hp
        elseif prioritize == "highest_level" then
            return a.level > b.level
        else
            return (a.hrp.Position - charPos).Magnitude < (b.hrp.Position - charPos).Magnitude
        end
    end)

    return candidates[1]
end

-------------------------------------------------
-- AUTO COLLECT TRÁI CÂY
-------------------------------------------------
local function FindNearestFruit()
    local char = GetCharacter()
    if not char then return nil end

    local closest = nil
    local shortest = math.huge
    local myPos = char.HumanoidRootPart.Position

    for _, obj in ipairs(workspace:GetChildren()) do
        if obj:IsA("Model") and (obj.Name:find("Fruit") or obj:FindFirstChild("Handle")) then
            local handle = obj:FindFirstChild("Handle") or obj:FindFirstChildWhichIsA("BasePart")
            if handle then
                local dist = (handle.Position - myPos).Magnitude
                if dist < shortest then
                    shortest = dist
                    closest = handle
                end
            end
        end
    end
    return closest
end

local function StoreFruit()
    pcall(function()
        ReplicatedStorage.Remotes.CommF_:InvokeServer("StoreFruit")
    end)
end

-------------------------------------------------
-- FAST ATTACK
-------------------------------------------------
task.spawn(function()
    while true do
        task.wait(getgenv().FastAttack and 0.05 or 0.2)
        if (getgenv().AutoQuest or getgenv().AutoFruit) and getgenv().FastAttack then
            pcall(function()
                local char = GetCharacter()
                if not char then return end
                local tool = char:FindFirstChildOfClass("Tool")
                if tool then
                    tool:Activate()
                end
            end)
        end
    end
end)

-------------------------------------------------
-- MENU
-------------------------------------------------
Tabs.Main:AddSection("Cài đặt Target")
Tabs.Main:AddDropdown("TargetPriority", {
    Title = "Ưu tiên chọn quái",
    Values = {"Gần nhất (Nearest)", "Yếu nhất (Weakest HP)", "Cấp cao nhất (Highest Lv)"},
    Default = "Gần nhất (Nearest)",
    Callback = function(Value)
        if Value:find("Nearest") then getgenv().TargetPrioritize = "nearest"
        elseif Value:find("Weakest") then getgenv().TargetPrioritize = "weakest"
        elseif Value:find("Highest") then getgenv().TargetPrioritize = "highest_level" end
    end
})

Tabs.Main:AddSlider("FarmDistance", {
    Title = "Độ cao lơ lửng",
    Default = 6, Min = 4, Max = 15, Rounding = 1,
    Callback = function(v) getgenv().FarmDistance = v end
})

Tabs.Main:AddSlider("TweenSpeed", {
    Title = "Tốc độ bay Farm",
    Default = 260, Min = 180, Max = 400, Rounding = 10,
    Callback = function(v) getgenv().TweenSpeed = v end
})

Tabs.Main:AddToggle("FastAttackToggle", {
    Title = "Fast Attack",
    Default = true,
    Callback = function(v) getgenv().FastAttack = v end
})

-------------------------------------------------
-- AUTO FARM LEVEL
-------------------------------------------------
Tabs.Main:AddSection("Auto Farm Level")
Tabs.Main:AddToggle("AutoQuestToggle", {
    Title = "Auto Farm Level (Quest + Nearest)",
    Default = false,
    Callback = function(Value)
        getgenv().AutoQuest = Value
        ToggleNoclip(Value)

        if not Value then
            isGoingToFarmArea = false
            ResetCharacterState()
            return
        end

        getgenv().AutoFruit = false -- tắt auto fruit khi bật farm

        task.spawn(function()
            while getgenv().AutoQuest do
                pcall(EquipWeapon)
                task.wait(0.7)
            end
        end)

        task.spawn(function()
            local noMobTimer = 0

            while getgenv().AutoQuest do
                task.wait(0.12)

                pcall(function()
                    local char = GetCharacter()
                    if not char then return end
                    local hrp = char.HumanoidRootPart

                    local questName, questLevel, mobName, npcCFrame, mobCFrame = GetQuestData()

                    if not HasQuest() then
                        isGoingToFarmArea = false
                        noMobTimer = 0

                        if npcCFrame then
                            local dist = (hrp.Position - npcCFrame.Position).Magnitude
                            if dist > 10 then
                                TweenTo(npcCFrame * CFrame.new(0, 0, 2.5))
                            else
                                StopTween()
                                for i = 1, 5 do
                                    pcall(function()
                                        ReplicatedStorage.Remotes.CommF_:InvokeServer("StartQuest", questName, questLevel)
                                    end)
                                    task.wait(0.12)
                                end
                                task.wait(1.3)
                            end
                        end
                        return
                    end

                    local target = FindBestTarget(hrp.Position, mobName, getgenv().TargetPrioritize, 5000)

                    if target then
                        isGoingToFarmArea = false
                        noMobTimer = 0

                        local pos = target.hrp.Position
                        local height = getgenv().FarmDistance or 6
                        local farmCF = CFrame.new(pos + Vector3.new(0, height, 0), pos)
                        TweenTo(farmCF)

                        pcall(function()
                            target.hrp.CanCollide = false
                            target.hrp.AssemblyLinearVelocity = Vector3.zero
                        end)
                    else
                        noMobTimer = noMobTimer + 0.12
                        if noMobTimer > 2.5 and mobCFrame then
                            isGoingToFarmArea = true
                            TweenTo(mobCFrame)
                        end
                    end
                end)
            end

            isGoingToFarmArea = false
            ResetCharacterState()
        end)
    end
})

-------------------------------------------------
-- AUTO COLLECT TRÁI CÂY
-------------------------------------------------
Tabs.Main:AddSection("Auto Collect Trái Cây")
Tabs.Main:AddToggle("AutoFruitToggle", {
    Title = "Auto Collect Trái Cây",
    Default = false,
    Callback = function(Value)
        getgenv().AutoFruit = Value
        ToggleNoclip(Value)

        if not Value then
            ResetCharacterState()
            return
        end

        getgenv().AutoQuest = false -- tắt farm khi đi lấy trái

        task.spawn(function()
            while getgenv().AutoFruit do
                task.wait(0.3)

                pcall(function()
                    local fruit = FindNearestFruit()

                    if fruit then
                        local targetCF = CFrame.new(fruit.Position + Vector3.new(0, 3, 0))
                        TweenTo(targetCF, getgenv().FruitTweenSpeed or 300)

                        local char = GetCharacter()
                        if char then
                            local dist = (char.HumanoidRootPart.Position - fruit.Position).Magnitude
                            if dist < 15 then
                                char.HumanoidRootPart.CFrame = CFrame.new(fruit.Position + Vector3.new(0, 2, 0))
                                task.wait(0.4)

                                if getgenv().AutoStoreFruit then
                                    StoreFruit()
                                    task.wait(0.4)
                                    StoreFruit()
                                end
                            end
                        end
                    end
                end)
            end
            ResetCharacterState()
        end)
    end
})

Tabs.Main:AddToggle("AutoStoreFruitToggle", {
    Title = "Tự động cất Trái (Store Fruit)",
    Default = true,
    Callback = function(v)
        getgenv().AutoStoreFruit = v
    end
})

Tabs.Main:AddSlider("FruitTweenSpeed", {
    Title = "Tốc độ bay lấy trái",
    Default = 300, Min = 200, Max = 450, Rounding = 10,
    Callback = function(v)
        getgenv().FruitTweenSpeed = v
    end
})

-------------------------------------------------
-- TAB NHÂN VẬT & SETTINGS
-------------------------------------------------
Tabs.Player:AddSlider("WalkSpeed", {
    Title = "WalkSpeed",
    Default = 16, Min = 16, Max = 300, Rounding = 0,
    Callback = function(Value)
        local char = GetCharacter()
        if char and char:FindFirstChild("Humanoid") then
            char.Humanoid.WalkSpeed = Value
        end
    end
})

SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)
InterfaceManager:SetFolder("DatkitsBF")
SaveManager:SetFolder("DatkitsBF")
InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)

Window:SelectTab(1)

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1)
    ToggleNoclip(false)
    ResetCharacterState()
end)

print("[Datkits Premium] Đã ghép Auto Collect Trái Cây thành công!")
