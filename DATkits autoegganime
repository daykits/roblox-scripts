repeat task.wait() until game:IsLoaded()

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "sleal egg | Anime",
   LoadingTitle = "egg!",
   LoadingSubtitle = "Auto Find Egg & Return Home",
   ConfigurationSaving = { Enabled = false },
   Discord = { Enabled = false },
   KeySystem = false
})

local MainTab = Window:CreateTab("Chính", 4483362458)
_G.AutoFarm = false

-- Hàm Teleport an toàn dùng PivotTo + tắt va chạm
local function safeTeleport(targetCFrame)
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    
    if character and rootPart then
        -- Tắt va chạm tạm thời để không bị giật lag/kẹt địa hình
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
        
        rootPart.AssemblyLinearVelocity = Vector3.zero
        rootPart.AssemblyAngularVelocity = Vector3.zero
        
        character:PivotTo(targetCFrame)
        task.wait(0.1)
        rootPart.AssemblyLinearVelocity = Vector3.zero
    end
end

-- Hàm lấy vị trí CFrame chuẩn của vật thể
local function getObjectCFrame(object)
    if object:IsA("BasePart") then
        return object.CFrame
    elseif object:IsA("Model") then
        return object:GetPivot()
    end
    return nil
end

-- Hàm TỰ ĐỘNG TÌM TRỨNG ở nhà người khác (Tránh vị trí nhà mình)
local function findTargetEggPrompt(homeCFrame)
    for _, prompt in ipairs(workspace:GetDescendants()) do
        if prompt:IsA("ProximityPrompt") and prompt.Enabled then
            local parent = prompt.Parent
            if parent then
                local eggCFrame = getObjectCFrame(parent)
                if eggCFrame then
                    -- Kiểm tra khoảng cách: Chỉ nhặt trứng ở cách xa Nhà (> 30 studs) để tránh nhặt nhầm nhà mình
                    local distFromHome = (eggCFrame.Position - homeCFrame.Position).Magnitude
                    if distFromHome > 30 then
                        return prompt, eggCFrame
                    end
                end
            end
        end
    end
    return nil, nil
end

-- Hàm tự động cầm trứng lên tay
local function equipEgg()
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local backpack = player:FindFirstChild("Backpack")
    
    if humanoid and backpack then
        for _, tool in ipairs(backpack:GetChildren()) do
            if tool:IsA("Tool") then
                humanoid:EquipTool(tool)
                break
            end
        end
    end
end

-- Nút Bật/Tắt trên UI
MainTab:CreateToggle({
   Name = "Tự Động Gom Trứng Về Nhà",
   CurrentValue = false,
   Flag = "AutoFarmToggle",
   Callback = function(Value)
      _G.AutoFarm = Value
   end,
})

-- Vòng lặp xử lý chính
task.spawn(function()
    local player = game.Players.LocalPlayer
    
    while true do
        task.wait(0.1)
        if _G.AutoFarm then
            local character = player.Character or player.CharacterAdded:Wait()
            local rootPart = character:FindFirstChild("HumanoidRootPart")
            
            if rootPart then
                -- 1. Lưu lại tọa độ Nhà của bạn (Vị trí đang đứng)
                local homeCFrame = rootPart.CFrame
                
                -- 2. Tự động quét tìm trứng trên bản đồ
                local prompt, eggCFrame = findTargetEggPrompt(homeCFrame)
                
                if prompt and eggCFrame then
                    -- 3. Teleport trực tiếp đến vị trí quả trứng tìm được (Dịch lên trên 2 studs để không kẹt đất)
                    safeTeleport(eggCFrame * CFrame.new(0, 2, 0))
                    task.wait(0.4)
                    
                    -- 4. Kích hoạt nút nhặt trứng
                    task.spawn(function()
                        if fireproximityprompt then
                            fireproximityprompt(prompt)
                        else
                            prompt:InputHoldBegin()
                            task.wait(prompt.HoldDuration + 0.1)
                            prompt:InputHoldEnd()
                        end
                    end)
                    task.wait(1)
                    
                    -- 5. Cầm trứng trên tay
                    equipEgg()
                    task.wait(0.3)
                    
                    -- 6. Teleport mang trứng quay trở về Nhà
                    safeTeleport(homeCFrame)
                    
                    -- 7. Chờ 4 giây trước khi đi trộm quả tiếp theo
                    task.wait(4)
                else
                    -- Nếu không tìm thấy trứng ở nhà khác
                    Rayfield:Notify({
                       Title = "Đang tìm trứng...",
                       Content = "Chưa tìm thấy trứng ở nhà khác hoặc trứng chưa xuất hiện.",
                       Duration = 2,
                       Image = 4483362458
                    })
                    task.wait(3)
                end
            end
        end
    end
end)

Rayfield:Notify({
   Title = "Đã tối ưu!",
   Content = "Đã chuyển sang chế độ Tự động quét vị trí Trứng thông minh.",
   Duration = 3,
   Image = 4483362458
})
